name: Descarga Automatica de Librerias 3D

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  descargar:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar codigo del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python en el servidor
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar libreria Requests
        run: pip install requests

      - name: Ejecutar script utilizando fuentes de GitHub corregidas
        run: |
          python -c '
          import os, requests
          os.makedirs("js", exist_ok=True)
          
          # URLs corregidas apuntando al subdominio raw.githubusercontent.com
          librerias = {
              "js/three.min.js": "https://raw.githubusercontent.com",
              "js/OrbitControls.js": "https://raw.githubusercontent.com",
              "js/GLTFLoader.js": "https://raw.githubusercontent.com"
          }
          
          for ruta, url in librerias.items():
              print(f"Descargando {ruta}...")
              res = requests.get(url, timeout=30)
              res.raise_for_status()
              
              contenido = res.text.strip()
              if contenido.startswith("<") or "Page Not Found" in contenido:
                  raise Exception(f"Error crítico: La URL devolvió un error para {ruta}")
                  
              with open(ruta, "w", encoding="utf-8") as f:
                  f.write(contenido)
          print("Librerias descargadas con éxito y de forma limpia.")
          '

      - name: Guardar cambios en el repositorio de GitHub
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@://github.com"
          git add js/
          git commit -m "Auto: Forzar descarga limpia con URLs corregidas" && git push || echo "No hay cambios nuevos"
